[package]
name = "adder"
version = "0.1.0"
edition = "2024"

[dependencies]
add_one = { path = "../add_one" }
serde = { workspace = true }  # version inherited from workspace
serde_json = { workspace = true }
